# Dependencias
node_modules/
npm-debug.log*

# Variables de entorno (¡NUNCA subir al repo!)
.env
.env.local
.env.*.local
.env.production

# Build output
# NOTA: dist/ SÍ se versiona a propósito — es el artefacto que se sube a
# Dinahosting (hosting estático). Rebuild con `npm run build` antes de subir.
dist-ssr/

# Vite cache
.vite/

# Editor
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db

# Backup/temp
*.bak
*.tmp

# API con credenciales reales (¡NUNCA subir!)
api_dinahosting/api_DEPLOY.php
migracion_*.sql
.vercel

# Claude Code
.claude/

# Skills instaladas (caveman, ponytail) — herramientas de desarrollo, no app
.agents/
skills-lock.json
